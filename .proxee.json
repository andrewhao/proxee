{
  "certificate_hostnames": ["localhost"],
  "hosts": {
    "app": "http://localhost:3000",
    "ui": "http://localhost:3001"
  },
  "rules": {
    "^http://.*/dashboard.*$": "ui",
    "^http://.*/_next.*$": "ui",
    ".*": "app"
  }
}
